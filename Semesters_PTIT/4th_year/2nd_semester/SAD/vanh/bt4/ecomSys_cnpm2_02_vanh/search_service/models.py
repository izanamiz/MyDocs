# from django.db import models
# from book.models import Book

# class SearchLog(models.Model):
#     searched_title = models.CharField(max_length=100, blank=True, null=True)
#     searched_author = models.CharField(max_length=100, blank=True, null=True)
#     search_timestamp = models.DateTimeField(auto_now_add=True)

#     def __str__(self):
#         return f"{self.searched_title} - {self.searched_author} - {self.search_timestamp}"
